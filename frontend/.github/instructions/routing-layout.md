# Routing & Layout Rules

- Layout is controlled via route.data.layout
- App component decides layout dynamically
- Never hardcode layout in feature components
- Use ROUTE_PATHS constants for navigation